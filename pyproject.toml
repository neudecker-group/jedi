[project]
name = "strainjedi"
description = "Judgement of Energy DIstribution - quantum chemical strain analysis"
version = "1.0.0"
readme = "README.rst"
license = "MIT"
license-files = [
    "LICENSE",
]
requires-python = ">=3.10"

dependencies = [
    "mpi4py>=2.0",
    "numpy",
    "ase",
    "matplotlib==3.10.3",
    "typing-extensions>=4.15.0",
]

classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3.10",
]

[project.scripts]
strainjedi = "strainjedi.main:main"

[project.urls]
Homepage = "https://github.com/neudecker-group/jedi"

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[dependency-groups]
docs = [
    "sphinx==8.1.3",
    "sphinx-rtd-theme==3.1.0",
    "numpy",
    "ase",
    "furo==2025.12.19",
    "sphinxcontrib-video==0.4.2",
]
lint = [
    "ruff",
]
test = [
    "pytest",
    "pyyaml",
]
types = [
    "ty",
]

[tool.setuptools]
packages = [
    "strainjedi",
    "strainjedi.io",
]

[tool.pytest]
addopts = ["-v", "--tb=long"]
testpaths = [
    "tests",
]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
ignore = [
    "E741", # do not lint ambigious variables names such as l, O, I
]
