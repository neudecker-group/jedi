VASP
====

At the moment there is no get_vibrations() function for the ASE VASP calculator in the pip installable version. Instead you can use the developer's version of ASE from github:

.. code-block:: console

    $ git clone https://gitlab.com/ase/ase.git